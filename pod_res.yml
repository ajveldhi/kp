
apiVersion: v1
kind: Pod

metadata:
  name: pod4
  labels:
    app: myapp1
    type: frontend

spec:
  containers:
  - name: mycont
    image: coolgourav147/nginx-custom
    resources:
      requests:
        memory: 1Mi
        cpu: 0.1
      limits:
        memory: 100Mi
        cpu: .5

